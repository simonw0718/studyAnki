{
  "name": "studyanki",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-static.mjs",
    "deploy": "npx wrangler deploy --assets=./dist",
    "dev": "python3 server.py"
  }
}
